import 'package:flutter/material.dart';

class MainEditPage extends StatelessWidget {
  const MainEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Edit'),
      ),
      body: Container(
        color: Colors.green,
      ),
    );
  }
}
